package armstrongNumberChecker;

public class ArmStrongChecker {
	public int armStrong(int n) {
		double ne=0;
		while(n!=0) {
			int last=n%10;
			ne=ne+Math.pow(last,3);
			n=n/10;
		}
		return ne;
	}

}
