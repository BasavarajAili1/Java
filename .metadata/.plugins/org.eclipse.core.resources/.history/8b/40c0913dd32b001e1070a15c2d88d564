package armstrongNumberChecker;

public class ArmStrongChecker {
	public int armStrong(int num, int count) {
		int sum=0;
		while(num!=0) {
			int rem=num%10;
			int nextnum=(int) Math.pow(rem, count);
			sum=sum+nextnum;
			num=num/10;
		}
		return sum;
	}
}
	
		
